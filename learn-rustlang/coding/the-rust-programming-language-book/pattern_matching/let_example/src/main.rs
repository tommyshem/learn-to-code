fn main() {
    // match tuple to variable names
    let (x, y, z) = (1, 2, 3);
    // print to stdout using the named variables
    println!("X: {}", x);
    println!("Y: {}", y);
    println!("Z: {}", z);
}
